Das Ziel ist es ein python script zu generieren, das uns synthetische Daten generiert, die wir für Tests und Entwicklung verwenden können. Wir brauchen folgende DAten:

- deutsche Ausweisnummern mit gültiger Prüfziffer
- deutsche steuer-IDs mit gültiger Prüfziffer

Es soll eine Funktion erstellt werden, welche noch folgende Parameter akzeptiert:
- Anzahl der zu generierenden Datensätze
- Ausgabeformat (z.B. CSV, JSON)

Erstelle alle notwendigen Dateien für diese Aufgabe mit pyproject.toml datei und wie wollen uv zum package management verwenden.